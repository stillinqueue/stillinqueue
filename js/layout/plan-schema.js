export const ROOM_DEFAULTS = {
  living: {
    minWidth: 12,
    minHeight: 14,
    preferredWidth: 14,
    preferredHeight: 16,
    zone: "public"
  },

  familyLounge: {
    minWidth: 10,
    minHeight: 12,
    preferredWidth: 12,
    preferredHeight: 14,
    zone: "semiPublic"
  },

  dining: {
    minWidth: 9,
    minHeight: 10,
    preferredWidth: 10,
    preferredHeight: 12,
    zone: "semiPublic"
  },

  kitchen: {
    minWidth: 8,
    minHeight: 10,
    preferredWidth: 10,
    preferredHeight: 10,
    zone: "service"
  },

  bedroom: {
    minWidth: 10,
    minHeight: 11,
    preferredWidth: 11,
    preferredHeight: 12,
    zone: "private"
  },

  masterBedroom: {
    minWidth: 11,
    minHeight: 12,
    preferredWidth: 12,
    preferredHeight: 14,
    zone: "private"
  },

  attachedToilet: {
    minWidth: 5,
    minHeight: 7,
    preferredWidth: 6,
    preferredHeight: 8,
    zone: "service"
  },

  commonToilet: {
    minWidth: 5,
    minHeight: 7,
    preferredWidth: 5,
    preferredHeight: 7,
    zone: "service"
  },

  utility: {
    minWidth: 5,
    minHeight: 6,
    preferredWidth: 6,
    preferredHeight: 8,
    zone: "service"
  },

  puja: {
    minWidth: 4,
    minHeight: 5,
    preferredWidth: 5,
    preferredHeight: 6,
    zone: "semiPublic"
  },

  corridor: {
    minWidth: 3.5,
    preferredWidth: 4,
    zone: "circulation"
  }
};
